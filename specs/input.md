# Input from StakeHolders

- Necesitamos una app donde podamos versionar prompts
- Los prompts quedan automaticamente disponibles para su consumo por API (usando APIKey)
- Los usuarios conectan github para que nosotros podamos armar un repo y ahi versionar por API